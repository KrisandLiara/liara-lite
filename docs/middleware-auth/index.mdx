# Middleware & Authentication

This section contains documentation about the middleware and authentication layers.

This section covers the security architecture of the Liara application. It details how users are authenticated, how their access to data is controlled, and the different layers of security that protect the system.

---

### Key Topics

- **<Keyword to="/app/system-overview/middleware-auth/supabase-jwt-logic">Supabase JWT Logic</Keyword>**: A step-by-step breakdown of how a user signs up, logs in, and receives a secure session token (JWT).

- **<Keyword to="/app/system-overview/middleware-auth/express-auth-middleware">Express Auth Middleware</Keyword>**: An explanation of the custom middleware used in the Express.js backend to protect routes. 